/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuração padrão do Next.js
  reactStrictMode: true,
  images: {
    domains: ['example.com'], // ajuste conforme a necessidade do seu projeto
  },
};
